apiVersion: v1
kind: Service
metadata:
  name: {{ i.Values.service.name }}
  labels:
    {{- include "KonaAI-API.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "KonaAI-API.selectorLabels" . | nindent 4 }}
