apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "KonaAI-API.fullname" . }}-test-connection"
  labels:
    {{- include "KonaAI-API.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "KonaAI-API.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
